CREATE COMPUTE MODULE Database_ESQL_Compute
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
		SET OutputRoot = InputRoot;
		INSERT INTO Database."IIBADMIN"."EMPLOYEE_RECORD"(FIRSTNAME, LASTNAME, SALARY, WORKDEPT, JOB)
		VALUES(InputRoot.JSON.Data.FIRSTNAME, InputRoot.JSON.Data.LASTNAME, InputRoot.JSON.Data.SALARY, InputRoot.JSON.Data.WORKDEPT, InputRoot.JSON.Data.JOB);
		RETURN TRUE;
	END;
END MODULE;